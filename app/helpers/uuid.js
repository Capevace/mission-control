/**
 * Function to generate a new UUID v4.
 * @module @helpers/uuid
 */

const { v4: uuid } = require('uuid');

/**
 * Function to generate a new UUID v4.
 * @returns {string}
 */
module.exports = uuid;